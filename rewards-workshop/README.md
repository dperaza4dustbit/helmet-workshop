# rewards-workshop

Participant starting point for the DevConf lab.

**Checklist:** open the **coordinator page** in your browser → expand **Lab activities**
when your instructor tells you (checkboxes persist in that tab).

Same content in the pod:

```bash
cat "$REWARDS_WORKSHOP_HOME/workshop-activities.md"
```

Instructor reference (do not copy blindly): [`../rewards-demo/`](../rewards-demo/)

The pod terminal opens here in `rewards-workshop` (no `cd` needed).
